server:
  port: 8081
  servlet:
    context-path: /api

spring:
  application:
    name: event-tracking-api

  datasource:
    url: jdbc:postgresql://localhost:5432/event-tracker
    username: postgres
    password: github-enriqueig-qwerty
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
    com.enriqueibarraig: DEBUG